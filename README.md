# Streamlit Smith Chart Tool v8.1

Standalone LC mode now displays only the calculated LC network results. It does not show an artificial Original ideal-through trace. Touchstone mode continues to show Original and Matched comparisons.

```bash
pip install -r requirements.txt
streamlit run app.py
```
